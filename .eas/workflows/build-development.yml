name: Create Development Builds

on:
  workflow_dispatch: {}
  push:
    branches:
      - develop

jobs:
  build_android:
    name: Build Android
    type: build
    params:
      platform: android
      profile: development

  build_ios:
    name: Build iOS
    type: build
    params:
      platform: ios
      profile: development
